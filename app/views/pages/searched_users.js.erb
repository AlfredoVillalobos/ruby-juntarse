$('.searched-users').html("<%= j render 'user-sports', users: @users %>")
mq = window.matchMedia( "(max-width: 500px)" );
if (mq.matches) {
  $('.left-sidebar').fadeOut();
  setTimeout(function(){
    $('.main').fadeIn();    
  },400)    
}