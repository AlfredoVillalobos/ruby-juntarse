$('.modal').remove()
$('.modal-backdrop').remove()
$('<%= j render "modals/modal-show-invitation", invitation: @invitation%>').modal()
data = {
  user_id: <%= current_user.id %>,
  events: <%= @events %>
}
App.activity.received(data)